version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
      target: build
    tty: true
    stdin_open: true
    volumes:
    - .:/go/github-gantt-api
    ports:
    - 8001:8001
    env_file:
      - .env